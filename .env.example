# Base URL backend API (tanpa trailing slash). Di Cloudflare set ke URL production.
# Cookie HTTP-only dikirim lintas-origin via credentials: 'include'.
PUBLIC_API_URL=http://localhost:3000

# Origin frontend untuk CORS backend (harus didaftarkan di CORS_ORIGIN backend).
PUBLIC_APP_ORIGIN=http://localhost:5173
